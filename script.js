const menuButton=document.querySelector('.menu'),nav=document.querySelector('.topbar nav');menuButton.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuButton.setAttribute('aria-expanded',String(open));menuButton.setAttribute('aria-label',open?'Close menu':'Open menu')});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menuButton.setAttribute('aria-expanded','false')}));document.getElementById('booking-form').addEventListener('submit',e=>{e.preventDefault();const d=new FormData(e.currentTarget),message=`Hi Dublin 15 Door2Airport, I'd like a taxi quote.\n\nName: ${d.get('name')}\nPhone: ${d.get('phone')}\nPickup: ${d.get('pickup')}\nDrop off: ${d.get('destination')}\nDate: ${d.get('date')}\nTime: ${d.get('time')}\nPassengers: ${d.get('passengers')}`;window.open(`https://wa.me/353858122981?text=${encodeURIComponent(message)}`,'_blank','noopener')});document.getElementById('year').textContent=new Date().getFullYear();const reviewButton=document.getElementById('review-button'),reviewHelp=document.getElementById('review-help');reviewButton.addEventListener('click',()=>{reviewHelp.hidden=false});let installPrompt;const installButton=document.getElementById('install-app'),installHelp=document.getElementById('install-help'),isIOS=/iphone|ipad|ipod/i.test(navigator.userAgent),isStandalone=window.matchMedia('(display-mode: standalone)').matches||navigator.standalone===true;if(isStandalone){installButton.textContent='App installed';installButton.disabled=true}window.addEventListener('beforeinstallprompt',event=>{event.preventDefault();installPrompt=event;installButton.classList.add('ready')});installButton.addEventListener('click',async()=>{if(installPrompt){installPrompt.prompt();const result=await installPrompt.userChoice;if(result.outcome==='accepted'){installButton.textContent='App installed';installButton.disabled=true}installPrompt=null}else{installHelp.innerHTML=isIOS?'In <strong>Safari</strong>, tap the <strong>Share</strong> button, then choose <strong>Add to Home Screen</strong> and tap <strong>Add</strong>.':'In <strong>Chrome</strong>, tap the <strong>⋮</strong> menu and choose <strong>Install app</strong> or <strong>Add to Home screen</strong>.';installHelp.hidden=false}});window.addEventListener('appinstalled',()=>{installButton.textContent='App installed';installButton.disabled=true});if('serviceWorker'in navigator)window.addEventListener('load',()=>navigator.serviceWorker.register('/service-worker.js'));
